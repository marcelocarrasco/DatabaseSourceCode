--------------------------------------------------------
--  DDL for View CORE_CISCO_L81B04_GGSN_MONTH
--------------------------------------------------------

  CREATE OR REPLACE VIEW "HARRIAGUE"."CORE_CISCO_L81B04_GGSN_MONTH" ("FECHA", "GWNAME", "SERVID", "SERVNAME", "DPCA_CCA_MESSAGES_REJECTED") AS 
  SELECT TRUNC( FECHA, 'MONTH') as FECHA, GWNAME,  SERVID, SERVNAME,
ROUND ((DECODE (SUM(DPCAMSGCCRINIT), 0, 0, SUM(DPCAMSGCCAINITREJ) / SUM(DPCAMSGCCRINIT))),2) as DPCA_CCA_MESSAGES_REJECTED
FROM CORE_CISCO_L81B04_GGSN_HIST
GROUP BY  TRUNC( FECHA, 'MONTH'), SERVID, SERVNAME,  GWNAME
;
