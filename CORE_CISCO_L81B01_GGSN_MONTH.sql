--------------------------------------------------------
--  DDL for View CORE_CISCO_L81B01_GGSN_MONTH
--------------------------------------------------------

  CREATE OR REPLACE VIEW "HARRIAGUE"."CORE_CISCO_L81B01_GGSN_MONTH" ("FECHA", "GWNAME", "SERVID", "SERVNAME", "CURRENT_ACTIVE_GX_SESS") AS 
  SELECT TRUNC( FECHA, 'MONTH') as FECHA, GWNAME, SERVID, SERVNAME, SUM(DPCACURSESS) AS CURRENT_ACTIVE_GX_SESS
FROM CORE_CISCO_L81B01_GGSN_HIST
GROUP BY  TRUNC( FECHA, 'MONTH'), SERVID, SERVNAME,GWNAME
;
